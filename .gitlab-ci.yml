debug:
  stage: build  # или любая другая стадия
  script:
    - echo "Checking available images..."
    - docker images 2>&1 || echo "Cannot run docker images"
    - env | grep -i image || echo "No IMAGE env"
  allow_failure: true